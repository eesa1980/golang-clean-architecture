{
  "name": "clean-architecture-golang",
  "scripts": {
    "preinstall": "cp .env.example .env",
    "install": "make install",
    "dev": "nodemon --watch ./pkg --exec make run --signal SIGTERM",
    "lint": "make lint"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
